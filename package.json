{
  "name": "quizapp-aws-sam",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev-start": "sam local start-api --env-vars env.json --skip-pull-image",
    "dev-build": "nodemon --exec sam build",
    "dev-deploy": "sam deploy"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.1345.0",
    "nodemon": "^2.0.22"
  }
}
